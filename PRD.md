# DeepNews — 单人可完成的新闻阅读器 PRD

## 项目概述

- **产品名称**：DeepNews
- **目标平台**：Android 8.0+（API 26+）
- **开发角色**：单人开发者
- **核心策略**：不建后端，客户端直接调用外部 API + LLM 实现深度阅读体验
- **核心价值**：用 AI 帮你「看穿一条新闻」，而非刷个不停

## 核心理念变化

| 原方案 | 现方案 |
|--------|--------|
| 自建爬虫 + 深度思考引擎 | 直接调用 NewsAPI + LLM API |
| 三层推理架构 | LLM 单次调用完成摘要/立场/实体提取 |
| 知识图谱 (Neo4j) | 本地 Room 缓存 + JSON 关系存储 |
| 因果关系/趋势预测 | **砍掉** |
| 社交/辩论/社区 | **砍掉** |
| 服务端渲染简报 | 客户端调用 LLM 生成 |

## 系统架构

```
┌─────────────────────────────────────────┐
│               Android App                │
│  ┌──────────┐  ┌──────────┐  ┌──────┐  │
│  │ NewsAPI   │  │  LLM API  │  │ Room  │  │
│  │ (Retrofit)│  │ (Claude)  │  │ 缓存  │  │
│  └──────────┘  └──────────┘  └──────┘  │
│         ↕              ↕                 │
│  ┌──────────────────────────────────┐   │
│  │   Repository Layer (数据聚合层)    │   │
│  └──────────────────────────────────┘   │
│         ↕                               │
│  ┌──────────────────────────────────┐   │
│  │   ViewModel + LiveData           │   │
│  └──────────────────────────────────┘   │
│         ↕                               │
│  ┌──────────────────────────────────┐   │
│  │   Jetpack Compose / View UI      │   │
│  └──────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

**没有自建后端**。所有数据通过客户端直连外部 API。

## 外部依赖

| 服务 | 用途 | 费用 |
|------|------|------|
| NewsAPI.org | 获取新闻文章（支持按关键词/来源/语言查询） | 免费版 100 req/day，单人足够 |
| Claude API / GPT-4o | 文章摘要、立场提取、事件聚类、每日简报 | 按 token 计费，$5-10/月 |
| Media Bias/Fact Check API | 信源偏见/可信度参考（可选） | 免费/低费 |

## V1.0 功能（2-3 个月可完成）

### P0 — 核心体验

| 功能 | 描述 | 实现方式 |
|------|------|----------|
| **事件聚合首页** | 按事件/主题分组展示新闻，而非单条信息流 | LLM 聚类 + NewsAPI 查询组合 |
| **多角度视图** | 同一事件展示多家媒体报道，标注各自立场 | LLM 提取立场 + 不同信源并排 |
| **AI 摘要** | 每条新闻/事件生成一句话摘要 + 关键事实 | LLM API 调用 |
| **源文直达** | 点击跳转原文网页（WebView） | 直接打开 URL |

### P1 — 深度功能

| 功能 | 描述 | 实现方式 |
|------|------|----------|
| **信源标注** | 标注媒体倾向性（左/中/右/权威） | 本地预置媒体库 + LLM 辅助判断 |
| **收藏/稍后读** | 保存文章到本地 | Room 数据库 |
| **关键词订阅** | 用户订阅感兴趣的话题，首页优先展示 | DataStore 偏好 + NewsAPI 查询 |
| **阅读历史** | 按时间线展示已读内容 | Room 数据库 |

### P2 — 增强功能

| 功能 | 描述 |
|------|------|
| **每日深度简报** | 每日自动生成一份今日 5 件大事的深度摘要（LLM 生成） |
| **离线缓存** | 已加载的新闻和摘要支持离线阅读 |
| **夜间模式** | Material3 DayNight 主题 |
| **阅读统计** | 统计每日阅读量、阅读来源多样性（信息茧房预警） |

### 砍掉的功能（不做）

| 功能 | 砍掉原因 |
|------|----------|
| 知识图谱可视化 | Canvas 开发成本高，单人耗时过长 |
| 因果推理/趋势预测 | 需要复杂模型，超出个人项目范围 |
| 事实核查数据库 | 需要大量标注数据和持续维护 |
| 用户账号系统 | 无后端，无需账号 |
| 观点广场/辩论/专家 | 社区运营非单人可行 |
| 个性化学习曲线 | 复杂度高，价值存疑 |

## 数据流

```
用户打开 App
    │
    ▼
首页展示缓存事件簇（Room）
    │
    ▼
后台刷新：NewsAPI 拉取最新文章
    │
    ▼
LLM API 分析：摘要 · 聚类 · 立场提取
    │
    ▼
写入 Room 缓存，更新 UI
    │
    ▼
用户点击事件 → 多角度视图
    │  ├── 不同信源文章列表
    │  ├── 各信源立场标注
    │  └── AI 生成的事件概览
```

## 技术栈

| 层 | 选型 | 说明 |
|------|------|------|
| UI | Jetpack Compose 或 View 系统 | 视个人 Compose 熟练度决定 |
| 架构 | MVVM + Repository | Android 标准实践 |
| 网络 | Retrofit + OkHttp | 调用 NewsAPI + LLM API |
| 本地存储 | Room + DataStore | 缓存 + 偏好设置 |
| 图片 | Coil | 轻量图片加载 |
| 依赖注入 | 手动或 Hilt | 项目不大，手动 DI 也行 |
| 序列化 | Gson / Moshi | JSON 解析 |
| LLM 客户端 | OkHttp 直连或 Anthropic SDK | 调用 Claude API |

## 里程碑（单人业余时间）

| 阶段 | 时间 | 交付物 |
|------|------|--------|
| **Week 1-2** | 基建 | 项目结构搭建、Retrofit + Room 接入、NewsAPI 调通 |
| **Week 3-4** | 首页 | 事件聚合列表、LLM API 接入、摘要生成 |
| **Week 5-6** | 详情 | 多角度视图、信源标注、WebView 阅读 |
| **Week 7-8** | 功能 | 收藏、订阅设置、阅读历史 |
| **Week 9-10** | 增强 | 每日简报、离线缓存、夜间模式 |
| **Week 11-12** | 打磨 | 错误处理、加载状态、性能优化、上架准备 |

## 非功能需求

| 指标 | 目标 |
|------|------|
| 冷启动 | < 2s |
| 新闻加载 | < 3s（含 LLM 分析时展示骨架屏） |
| LLM 响应 | < 5s（异步加载，不阻塞 UI） |
| 离线支持 | 缓存最近 50 条新闻 + 摘要 |
| 崩溃率 | < 0.5% |

## 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| NewsAPI 免费额度不足 | 功能受限 | 100次/天，开发测试够用；上线后考虑升级付费 |
| LLM API 费用 | 持续成本 | 缓存 LLM 结果，同一新闻不重复请求 |
| API 接口变更 | 功能中断 | 抽象 API 层，方便切换供应商 |
| 单人进度慢 | 失去动力 | 先做 P0，跑通核心闭环后再加功能 |
| 内容版权 | 法律风险 | 仅展示摘要 + 原文链接，不存储全文 |
