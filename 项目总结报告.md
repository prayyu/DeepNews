# DeepNews 新闻聚合应用 — 项目总结报告

## 1. 项目概述

**DeepNews** 是一款基于 Android 平台的 AI 驱动新闻聚合应用，支持多平台热搜浏览、智能事件聚类、个性化关键词订阅和深色模式等功能。适用于 3-4人 小组作业。

- **应用名称**: DeepNews
- **包名**: `com.example.myapplication`
- **目标 SDK**: Android 14 (API 36)
- **最低 SDK**: Android 8.0 (API 26)
- **开发语言**: Java 11
- **构建系统**: Gradle Kotlin DSL + 版本目录

---

## 2. 运行截图 / 主要界面

| 界面 | 说明 |
|---|---|
| 闪屏页 | DeepNews 启动画面，1.5 秒后进入首页 |
| 首页 | TabLayout 切换频道 + 搜索栏 + 新闻事件列表 |
| 事件详情 | 事件标题、摘要、多角度报道列表 |
| 文章阅读 | 内置 WebView 加载原文，支持书签和分享 |
| 书签页 | 收藏文章列表，支持左滑删除、下拉刷新 |
| 每日简报 | AI 生成当日深度简报 |
| 设置页 | 深色模式、关键词订阅、关于信息 |

---

## 3. 技术栈

### 3.1 UI 层

| 技术 | 用途 |
|---|---|
| Material 3 (Material Components) | 全局 UI 主题，支持 DayNight 主题切换 |
| ConstraintLayout | 所有布局使用，适配 edge-to-edge |
| RecyclerView + CardView | 列表展示新闻事件、文章、书签 |
| TabLayout | 频道切换（今日头条 / 微博热搜 / 百度热榜） |
| BottomNavigationView | 底部导航（深度简报 / 收藏 / 设置） |
| SwipeRefreshLayout | 下拉刷新 |
| ItemTouchHelper | 书签左滑删除 |
| ViewModel + LiveData | MVVM 架构 |
| EditText + TextWatcher | 搜索栏实时触发 |
| LayoutAnimation | 列表项淡入动画 |

### 3.2 数据层

| 技术 | 用途 |
|---|---|
| Room 2.6.1 | 本地数据库，3 张表（articles / events / bookmarks） |
| SharedPreferences | 已读标记、深色模式、关键词订阅、简报缓存 |
| Gson 2.10.1 | JSON 序列化/反序列化 |

### 3.3 网络层

| 技术 | 用途 |
|---|---|
| OkHttp 4.12 | HTTP 客户端（HotListClient / LlmClient） |
| Retrofit 2.9 | HTTP 客户端（NewsAPI，备用数据源） |
| OkHttp Logging Interceptor | 调试日志 |

### 3.4 AI 能力

| 技术 | 用途 |
|---|---|
| DeepSeek Chat API | 新闻聚类、每日简报生成 |

### 3.5 架构模式

**MVVM + Repository**：

```
View (Activity) → ViewModel (LiveData) → Repository → API Client / Room DB
```

---

## 4. 文件结构

```
MyApplication/
├── app/
│   ├── build.gradle.kts            # 模块构建配置
│   └── src/main/
│       ├── AndroidManifest.xml     # 组件注册、权限
│       ├── java/com/example/myapplication/
│       │   ├── DeepNewsApp.java           # Application（恢复深色模式）
│       │   ├── MainActivity.java          # 首页
│       │   ├── SplashActivity.java        # 闪屏页
│       │   ├── api/
│       │   │   ├── HotListClient.java     # 多平台热榜 API
│       │   │   ├── LlmClient.java         # DeepSeek LLM 客户端
│       │   │   ├── NewsApi.java           # NewsAPI Retrofit 接口
│       │   │   ├── NewsArticle.java       # 新闻文章模型
│       │   │   ├── NewsResponse.java      # NewsAPI 响应模型
│       │   │   ├── RetrofitClient.java    # Retrofit 单例
│       │   │   └── EventCluster.java      # LLM 聚类结果模型
│       │   ├── data/
│       │   │   ├── AppDatabase.java       # Room 数据库
│       │   │   ├── NewsDao.java           # 文章 DAO
│       │   │   ├── EventDao.java          # 事件 DAO
│       │   │   ├── BookmarkDao.java       # 书签 DAO
│       │   │   ├── NewsRepository.java    # 数据仓库
│       │   │   └── entity/
│       │   │       ├── ArticleEntity.java
│       │   │       ├── EventEntity.java
│       │   │       └── BookmarkEntity.java
│       │   ├── ui/
│       │   │   ├── bookmark/
│       │   │   │   ├── BookmarkActivity.java
│       │   │   │   └── BookmarkListAdapter.java
│       │   │   ├── brief/
│       │   │   │   └── DailyBriefActivity.java
│       │   │   ├── detail/
│       │   │   │   ├── DetailActivity.java
│       │   │   │   └── ArticleListAdapter.java
│       │   │   ├── home/
│       │   │   │   ├── MainViewModel.java
│       │   │   │   ├── MainViewModelFactory.java
│       │   │   │   ├── EventAdapter.java
│       │   │   │   └── NewsAdapter.java
│       │   │   ├── settings/
│       │   │   │   ├── SettingsActivity.java
│       │   │   │   └── AboutActivity.java
│       │   │   └── webview/
│       │   │       └── WebViewActivity.java
│       │   └── util/
│       │       ├── BadgeUtils.java         # 彩色源标识生成
│       │       ├── Logger.java             # 日志工具
│       │       ├── ReadTracker.java        # 已读跟踪
│       │       ├── Result.java             # 结果封装
│       │       └── TimeUtils.java          # 相对时间格式化
│       └── res/
│           ├── anim/                       # 列表动画
│           ├── drawable/                   # 图标、骨架形状
│           ├── layout/                     # 13 个布局文件
│           ├── menu/                       # 底部导航菜单
│           ├── values/                     # 颜色、字符串、主题
│           └── values-night/               # 深色主题
├── gradle/
│   ├── libs.versions.toml                 # 版本目录
│   └── wrapper/
├── build.gradle.kts                       # 根构建配置
├── settings.gradle.kts
├── gradle.properties
└── 项目总结报告.md
```

---

## 5. 功能清单

### 核心功能

| 功能 | 状态 | 说明 |
|---|---|---|
| 多平台热搜浏览 | ✓ | 今日头条 / 微博热搜 / 百度热榜 |
| 事件聚类 | ✓ | DeepSeek LLM 将相似新闻聚合为事件 |
| 多角度报道 | ✓ | 每个事件展示多个信源的报道 |
| 内置浏览器 | ✓ | WebView 加载原文，支持 JS |
| 书签收藏 | ✓ | 收藏/取消/左滑删除/下拉刷新 |

### AI 功能

| 功能 | 状态 | 说明 |
|---|---|---|
| LLM 新闻聚类 | ✓ | 将同事件文章自动分组 |
| AI 每日简报 | ✓ | DeepSeek 生成当日深度新闻摘要 |
| 关键词优先排序 | ✓ | 订阅关键词相关新闻置顶 |

### 个性化

| 功能 | 状态 | 说明 |
|---|---|---|
| 深色模式 | ✓ | 手动切换，持久化保存 |
| 关键词订阅 | ✓ | 添加/清空关键词 |
| 已读标记 | ✓ | 已读文章标题变淡，显示"已读"标签 |
| 新闻搜索 | ✓ | 本地缓存实时搜索（>=2 字符触发） |

### UI/UX

| 功能 | 状态 | 说明 |
|---|---|---|
| 闪屏页 | ✓ | 1.5 秒品牌展示 |
| 骨架屏加载 | ✓ | 加载时显示灰色占位卡片 |
| 下拉刷新 | ✓ | 手动刷新新闻列表 |
| 列表动画 | ✓ | 条目淡入上移动画 |
| 底部导航 | ✓ | 深度简报 / 收藏 / 设置 |
| 彩色的源标识 | ✓ | 每条新闻左侧彩色圆形信源首字符 |
| 关于页面 | ✓ | 版本号、数据来源、团队信息 |
| 分享 | ✓ | 文章分享到其他应用 |

---

## 6. 数据流

```
                   ┌─────────────┐
                   │   uapis.cn  │
                   │  热榜 API    │
                   └──────┬──────┘
                          │ OkHttp
                          ▼
                   ┌─────────────┐
                   │HotListClient│
                   └──────┬──────┘
                          │ List<NewsArticle>
                          ▼
                   ┌─────────────┐     ┌─────────────┐
                   │    LLM?     │ ──► │DeepSeek Chat│
                   │  有 API Key?│     │   聚类 API   │
                   └──────┬──────┘     └──────┬──────┘
                          │                   │
               ┌──────────┘                   │
               ▼                              │
        ┌──────────────┐                      │
        │  按信源分组   │                      │
        │ (fallback)   │                      │
        └──────┬───────┘                      │
               │                              │
               └──────────┬───────────────────┘
                          │ List<EventCluster>
                          ▼
                   ┌─────────────┐
                   │NewsRepository│
                   └──────┬──────┘
                          │
                 ┌────────┴────────┐
                 ▼                 ▼
          ┌──────────┐     ┌──────────────┐
          │ Room DB  │     │MainViewModel │
          │(缓存/搜索)│     │  (LiveData)  │
          └──────────┘     └──────┬───────┘
                                  │ observe()
                                  ▼
                          ┌──────────────┐
                          │ MainActivity │
                          │ EventAdapter │
                          └──────────────┘
```

---

## 7. 数据存储

| 存储方式 | 数据内容 |
|---|---|
| Room `articles` 表 | 文章标题、URL、来源、描述、图片、事件分组 |
| Room `events` 表 | 事件标题、摘要、文章列表(JSON)、信源列表(JSON) |
| Room `bookmarks` 表 | 书签 URL、标题、来源、时间戳 |
| SharedPreferences `deepnews_prefs` | 深色模式(key)，关键词订阅(keywords)，已读URL(read_articles)，简报缓存(daily_brief) |

---

## 8. 已完成的改进（按时间顺序）

1. 修复 NewsAPI 国内不可用 → 切换为 uapis.cn 国内热榜
2. 修复 Anthropic API 国内不可用 → 切换 DeepSeek Chat API
3. 修复 DailyBriefActivity ClassCastException
4. 重构为 MVVM 架构（ViewModel + LiveData + Repository）
5. 支持多频道切换（TabLayout: 头条/微博/百度）
6. 添加搜索功能（本地 Room 缓存实时搜索）
7. 添加骨架屏加载动画
8. 添加列表动画
9. 底部导航栏替换文字按钮
10. 添加关于页面
11. 书签页添加左滑删除 + 下拉刷新
12. 深色模式持久化（Application 启动恢复）
13. 添加闪屏页
14. 修复 LLM max_tokens 过小导致崩溃
15. 添加彩色源标识（新闻卡片左侧圆形首字符图标）
16. 添加已读标记（文章阅读后自动标记，列表变淡）
17. 上传阿里云效 Codeup 代码托管

---

## 9. 可扩展方向

| 功能 | 说明 | 工作量 |
|---|---|---|
| 新闻缩略图（真实图片） | 找提供图片的 API 或用 jsoup 从原文提取 | 2-3天 |
| 阅读历史 | 在设置中展示已读文章历史记录 | 1天 |
| 字体大小调节 | 设置页添加字体滑块 | 0.5天 |
| 用户引导页 | 首次启动 ViewPager 引导 | 1天 |
| 错误重试按钮 | 加载失败时显示重试按钮 | 0.5天 |
| 自动深色模式 | 跟随系统自动切换 | 0.5天 |
| 单元测试 | ViewModel、Repository 单元测试 | 2天 |
| 导出书签 | 收藏导出为文本文件 | 0.5天 |
| 离线缓存 | 文章缓存到本地，无网络可读 | 2天 |

---

## 10. 团队成员分工参考（3-4人）

| 角色 | 负责模块 | 技术点 |
|---|---|---|
| 成员1 | UI / 首页 / 导航 | TabLayout、RecyclerView、动画、BottomNav |
| 成员2 | 数据 / 网络 / API | Room、OkHttp、Gson、热榜 API 对接 |
| 成员3 | LLM / AI 功能 | DeepSeek API、聚类算法 prompt、每日简报 |
| 成员4 | 个性化 / 设置 / 书签 | SharedPreferences、深色模式、WebView、书签管理 |

---

## 11. 项目配置

| 配置项 | 值 |
|---|---|
| compileSdk | 36 |
| minSdk | 26 |
| targetSdk | 36 |
| Java | 11 |
| Gradle | Kotlin DSL |
| 包名 | `com.example.myapplication` |
| 远程仓库 | https://codeup.aliyun.com/69c4a166405bafb07e124740/news.git |

---

*生成日期: 2026-05-14*
